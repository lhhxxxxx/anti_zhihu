# anti_zhihu

知乎小程序转链接

## 使用方法

1. 下载或 `git clone` 本插件：

在 `HoshinoBot\hoshino\modules` 目录下使用以下命令拉取本项目

```bash
git clone https://github.com/lhhxxxxx/anti_zhihu.git
```

2. 启用：

在 `HoshinoBot\hoshino\config\bot.py` 文件的 `MODULES_ON` 加入 `'anti_zhihu'`

然后重启 HoshinoBot
